# Simple Story Generator

A simple command-line program that generates a short story based on user-provided inputs for a character, setting, and problem.

## Usage

Run `python main.py` and follow the prompts to enter a character, setting, and problem. The program will then generate a short story based on your input.
