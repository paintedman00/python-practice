# Simple Dice Roller

A command-line tool for rolling dice.

## Usage

Run `python main.py` and follow the prompts to specify the number of dice and the number of sides per die.

Example:

```
$ python main.py
How many dice do you want to roll? 2
How many sides per die? 6
Rolls: [3, 5]
Sum: 8
```
