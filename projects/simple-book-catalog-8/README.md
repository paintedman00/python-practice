# Simple Book Catalog

A simple command-line tool to manage a small book catalog.

## Usage

Run `python main.py` and follow the on-screen instructions.

## Features

*   Add a book to the catalog (title, author).
*   View the current book catalog.
