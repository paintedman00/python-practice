# Simple Word Scrambler

A simple command-line tool that scrambles the letters of a word entered by the user.

## Usage

Run the script from the command line:

```bash
python main.py
```

The program will prompt you to enter a word. After you enter a word and press Enter, the program will output the scrambled word.
