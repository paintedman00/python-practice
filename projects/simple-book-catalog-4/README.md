# Simple Book Catalog

A simple command-line tool to manage a small book catalog.

## Usage

Run `python main.py` to start the program.

## Features

*   Add a new book to the catalog (title, author).
*   List all books in the catalog.
