# Grocery List Manager

This simple Python program allows you to manage a grocery list from the command line.

## Usage

Run the script with `python main.py` and follow the prompts to add, remove, and view items on your grocery list.
