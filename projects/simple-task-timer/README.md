# Simple Task Timer

A simple command-line tool to time tasks.

## Usage

Run `python main.py` and follow the prompts to start and stop timers for different tasks.

## Example

```
python main.py
```
