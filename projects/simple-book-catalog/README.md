# Simple Book Catalog

A command-line tool to manage a simple book catalog.

## Usage

Run `python main.py` and follow the on-screen prompts.

Options:
*   `add`: Add a new book to the catalog.
*   `list`: List all books in the catalog.
*   `search`: Search for a book by title.
*   `exit`: Exit the program.
